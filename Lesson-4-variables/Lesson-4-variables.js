let message;

message = `Hello`; //saving string `Hello` in a variable with the name message

let message;

message = `Hello!`;

alert(message); // Shows the contents of the variable

let message = `Hello!`; //identifying the variable and assigning some meaning to it

alert(message); // Hello!

let user = `John`, age = 25, message = `Hello`;

let user = `John`
let age = 25
let message = `Hello`

let user = `John`
    , age = 25
    , message = `Hello!`;

var message = `Hello!`

let message;

message = `Hello!`;

message = `World!`; // Meaning is changed

alert(message);

let hello = `Hello world!`;

let message;

// Copying the meaning `Hello world` from variable hello into the variable message
message = hello;

//Now two variables contain the same data
alert(hello); // Hello world!
alert(message); //Hello world!

let userName;
let test123;

let $ = 1; // creating a variable with the name "$"
let _ = 2; // creating a variable with the name "_"

alert($ + _); //3

let 1a; // variables cannot start with a number

let my-name; // "-" is not allowed in a name

const myBirthday = `04.08.2003`;

myBirthday = `01.01.2001` //error, since const cannot be changed!

const COLOR_RED = "#F00";
const COLOR_GREEN = "#0F0";
const COLOR_BLUE = "#00F";
const COLOR_ORANGE = "#FF7F00";

// ...when we have to choose a color
let color = COLOR_ORANGE
alert(color); // #FF7F00

const pageLoadTime = // Time, which the web-page takes to load